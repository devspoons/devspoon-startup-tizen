# 예시 파일 — 실제 공개키는 저장소에 커밋하지 않는다(같은 폴더 .gitignore 가 authorized_keys·id_rsa* 제외).
# tizen-env 의 root 로 SSH 접속할 공개키를 한 줄에 하나씩 담은 authorized_keys 를 준비한다.
# 형식 예: ssh-ed25519 AAAA...(키 본문) user@host
# 권한 600 (sshd StrictModes). 키 주입 방식은 README 의 tizen-env 절을 따른다.
